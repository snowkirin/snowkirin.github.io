---
import { siteLocale } from '../i18n/ui';

interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(siteLocale.intl, {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
